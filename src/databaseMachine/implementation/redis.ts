import { assign } from "xstate";

const redisImplementation = {
    services:{
        
        connectToDatabase:({port,url}:any,event:any)=>(send:any)=>{
              
        },
        listeningDatabase:({socket}:any,event:any)=>(send:any)=>{
            
            
        }
    },
    actions:{
       
        
    }
}


export default redisImplementation;